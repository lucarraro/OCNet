OCN_to_SSN <- function (OCN, ...) 
{
message("In `OCNet` v1.2.0, this function is disabled following the
  archival of the SSN package from CRAN (due to the retirement of rgdal). 
  A new OCN_to_SSN function will be included in the next release of OCNet. \n",
        appendLF=FALSE)
}